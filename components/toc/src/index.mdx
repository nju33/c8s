---
name: '@c8s/toc'
route: /components/toc
---

import {Playground, PropsTable} from 'docz';
import {Toc} from './organisms';

## @c8s/toc

<Playground>
  {() => {
    const toc = new Toc();
    const AComponent = props => {
      console.log('props: ', props);
      return <div ref={props.ariaRef} style={{marginBottom: 1000}}>{props.title}</div>
    }
    const items = [
      {title: 'foo'},
      {title: 'bar'},
      {title: 'baz'},
    ];

    return (
      <Toc.provider toc={toc}>
        <>
          <ul>
            <Toc.consumer>
              {({items}) => {
                return items.map(item => <li key={item.title}>{item.title}</li>)
              }}
            </Toc.consumer>
          </ul>
        </>
        <div>
          {items.map(toc.bind(AComponent))}
        </div>
      </Toc.provider>
    );
  }}
</Playground>
