---
name: '@c8s/toc'
route: /components/toc
---

import {Playground, PropsTable} from 'docz';
import {Theme, theme} from '@c8s/theme';
import {Toc} from './organisms';

## @c8s/toc

<Playground>
  {() => {
    const toc = new Toc();
    const AComponent = props => {
      return <div>{props.title}</div>
    }
    const items = [
      {title: 'foo'},
      {title: 'bar'},
      {title: 'baz'},
    ];

    return (
      <Theme theme={theme}>
        <>
          {items.map(toc.bind(AComponent))}
        </>
      </Theme>
    );
  }}
</Playground>
